sdk-sample-app:
  base-url: ${wiremock_base-url}
  access-token: test
  signature:
    rsa:
#      path:
      type: PKCS12
      password: "testpass"
      privateKey:
        alias: "test"
        password: "testpass"
      certificate:
        alias: "test"